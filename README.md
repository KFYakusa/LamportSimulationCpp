# LamportSimulationCpp


To run these files, please configure the path to your gcc compiler and to debuger gdb into the `.json`files, 


if want to add folders to root, have to add their path into `tasks.json`
